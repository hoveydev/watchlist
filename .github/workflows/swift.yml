# This workflow will build a Swift project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-swift

name: Swift

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: macos-13

    steps:
    - uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '15.0.1'
    - name: Setup swift
      uses: actions/checkout@v3
    - name: Clean build
      run: rm -rf .build
    - name: Build
      run: swift build -v
    - name: Run all tests
      run: xcodebuild test -scheme WatchList -destination 'platform=iOS Simulator,OS=17.0.1,name=iPhone 15 Pro'
    - name: Run unit tests
      run: swift test -v
